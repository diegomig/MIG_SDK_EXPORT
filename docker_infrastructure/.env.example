# ========================================
# MIG TOPOLOGY SDK - CONFIGURACIÓN
# ========================================

# Base de Datos PostgreSQL
POSTGRES_PASSWORD=mig_topology_pass
POSTGRES_PORT=5432
DATABASE_URL=postgresql://mig_topology_user:mig_topology_pass@localhost:5432/mig_topology?options=-c%20client_encoding=utf8

# Redis
REDIS_PORT=6379
REDIS_URL=redis://localhost:6379

# RPC Configuration (Arbitrum One)
# For testing, you can use public RPC endpoints or your own node
# Example public endpoints (replace with your preferred provider):
# SDK_RPC_HTTP_URLS=https://arb1.arbitrum.io/rpc,https://arbitrum-one-rpc.publicnode.com
# SDK_RPC_WS_URLS=wss://arb1.arbitrum.io/ws
# 
# For production, use your own RPC node or paid provider endpoints
# Format: comma-separated URLs or JSON array
SDK_RPC_HTTP_URLS=
SDK_RPC_WS_URLS=

# Logging (opcional)
RUST_LOG=info
RUST_BACKTRACE=1

# ========================================
# NOTAS DE SEGURIDAD
# ========================================
# - Cambiar POSTGRES_PASSWORD en producción
# - No commitear archivos .env con credenciales reales
# - Usar secretos de Docker o variables de entorno del sistema en producción
